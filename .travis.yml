language: csharp
branches:
  only:
  - dev
dist: trusty
mono: none
dotnet: 3.0.0-preview3-27503-5
os:
- linux
- osx
addons:
  apt:
    packages:
    - libunwind8
script:
- "./build.sh --ci /p:BuildNumber=$((10000+TRAVIS_BUILD_NUMBER))\\;DisableNet461Tests=true"
